# CORE PRIME 최종 검수본 적용 방법

프로그래머가 아니어도 적용할 수 있도록 **3단계만** 남겼습니다.
중간 STEP ZIP과 중간 SQL은 더 이상 따로 적용하지 마세요.

> 전제: 운영 Supabase DB에는 STEP 6A / STEP 6B-1이 이미 적용되어 있습니다.

## 1단계 — Supabase SQL 한 번 실행

1. Supabase 프로젝트 접속
2. 왼쪽 **SQL Editor** → **New query**
3. 이 프로젝트의 `supabase/FINAL_APPLY_ONCE.sql` 파일을 엽니다.
4. 내용을 처음부터 끝까지 전부 복사합니다.
5. SQL Editor에 붙여넣고 **Run**을 누릅니다.
6. 빨간 오류 없이 끝나면 완료입니다.

**오류가 나오면 같은 SQL을 반복 실행하지 말고 오류 문구 전체를 보내주세요.**

## 2단계 — GitHub Desktop으로 올리기

1. 기존 CORE PRIME 프로젝트 폴더를 별도 백업합니다.
2. 이 최종 ZIP의 프로젝트 파일로 교체합니다.
3. GitHub Desktop에서 CORE PRIME 저장소를 엽니다.
4. Summary: `CORE PRIME final reviewed release`
5. **Commit to main** → **Push origin**

## 3단계 — Vercel 확인

Vercel 배포 상태가 초록색 **Ready**인지 확인합니다.
첫 배포에서는 `unpdf` 설치와 Next.js production build가 실제 서버에서 정상 완료되는지 꼭 확인합니다.

---

# 관리자에서 PDF 자료를 반영하는 실제 순서

`/admin/imports`에서 진행합니다.

1. **PDF 업로드**
2. 상세 화면에서 **PDF 원문 추출 시작**
3. `다음 10페이지 처리` 또는 `최대 50페이지 연속 처리` 사용
4. 전체 추출 완료 후 **1. 건물·공실 후보 만들기**
5. **2. 기존 건물 자동매칭**
6. 미매칭 건물은 기존 건물에 연결하거나 **신규 등록**
7. **3. 공실 변경사항 계산**
8. 화면의 **최종 승인 전 공실 데이터 확인** 표에서 층·면적·임대료·관리비·입주시기와 원본 PDF를 대조
9. 충돌 공실 및 이번 자료에서 사라진 공실의 처리 방식을 선택
10. 문제가 없을 때만 **4. 최종 승인·반영**

마지막 승인 전에는 고객용 건물·공실 DB를 변경하지 않습니다.

---

# 최종 확인 권장 순서

처음부터 300페이지 전체를 승인하지 말고 아래 순서로 확인하는 것을 권장합니다.

1. NAI PDF 1개 업로드
2. 원문 추출 완료
3. 건물/공실 후보 생성
4. `랜드마크타워`, `루첸타워`, `해성1빌딩`, `707타워` 등 몇 개를 원본 PDF와 비교
5. 매칭/Diff 결과 확인
6. 이상이 없으면 전체 승인
7. 이후 CBRE → C&W 순으로 같은 방식 검증

---

# 안전 원칙

- PDF 원본은 private `source-pdfs` 버킷에 보관
- 원문과 추출 결과는 관리자 전용
- Parser가 고객용 canonical DB에 직접 기록하지 않음
- `PDF → 원문추출 → Staging → Matching → Diff → 관리자 검수 → 승인` 순서 유지
- 기존 공실이 새 자료에서 보이지 않아도 자동 삭제하지 않음
- 신규 건물은 Import에서 생성해도 기본 **비공개**
- Prime Score가 검증되지 않았으면 고객에게 숫자 0점 대신 **평가 준비중** 표시
