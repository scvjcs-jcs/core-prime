"use client";

import { useMemo, useState } from "react";
import { applyListingCorrection, bulkVerifyListingWarningsNoChange, getSignedPdfUrl, verifyListingWarningNoChange, type ListingCorrectionPayload } from "@/app/admin/(protected)/imports/actions";

type Values={floor:string|null;unit:string|null;gross_area:number|null;gross_area_py:number|null;exclusive_area:number|null;exclusive_area_py:number|null;deposit_per_py:number|null;rent_per_py:number|null;maintenance_per_py:number|null;noc_per_py:number|null;deposit_total_won:number|null;monthly_rent_total_won:number|null;management_fee_total_won:number|null;move_in_text:string|null;status:string|null;is_published:boolean|null};
type CorrectionRow={stagingListingId:string;listingId:string|null;buildingName:string;floor:string|null;page:number|null;reasons:string[];current:Values|null;extracted:Values;correctionStatus:"PENDING"|"APPLIED"|"VERIFIED_NO_CHANGE"|null;note:string|null};
const PAGE_SIZE=25;
const numberKeys:Array<keyof Values>=["gross_area","gross_area_py","exclusive_area","exclusive_area_py","deposit_per_py","rent_per_py","maintenance_per_py","noc_per_py","deposit_total_won","monthly_rent_total_won","management_fee_total_won"];
function format(v:unknown){if(v===null||v===undefined||v==="")return"-";if(typeof v==="number")return new Intl.NumberFormat("ko-KR",{maximumFractionDigits:2}).format(v);return String(v)}
function initialForm(row:CorrectionRow):Record<string,string|boolean>{const source=row.current??row.extracted;const out:Record<string,string|boolean>={};for(const[k,v]of Object.entries(source))out[k]=typeof v==="boolean"?v:v===null||v===undefined?"":String(v);return out}
function normalizedReason(row:CorrectionRow){return row.reasons[0]??"기타 경고"}

export default function ListingCorrectionReviewPanel({sourceDocumentId,rows}:{sourceDocumentId:string;rows:CorrectionRow[]}){
 const[query,setQuery]=useState(""),[status,setStatus]=useState("미검토"),[reason,setReason]=useState("전체 유형"),[openId,setOpenId]=useState<string|null>(null),[forms,setForms]=useState<Record<string,Record<string,string|boolean>>>({}),[notes,setNotes]=useState<Record<string,string>>({}),[busy,setBusy]=useState<string|null>(null),[message,setMessage]=useState<string|null>(null),[page,setPage]=useState(1),[selected,setSelected]=useState<string[]>([]);
 const counts=useMemo(()=>({pending:rows.filter(r=>!r.correctionStatus||r.correctionStatus==="PENDING").length,applied:rows.filter(r=>r.correctionStatus==="APPLIED").length,verified:rows.filter(r=>r.correctionStatus==="VERIFIED_NO_CHANGE").length}),[rows]);
 const reasonOptions=useMemo(()=>["전체 유형",...Array.from(new Set(rows.map(normalizedReason)))],[rows]);
 const filtered=useMemo(()=>rows.filter(r=>{const q=query.trim().toLowerCase(),qok=!q||`${r.buildingName} ${r.floor??""} ${r.reasons.join(" ")}`.toLowerCase().includes(q),sok=status==="전체"||(status==="미검토"&&(!r.correctionStatus||r.correctionStatus==="PENDING"))||(status==="수정반영"&&r.correctionStatus==="APPLIED")||(status==="정상확인"&&r.correctionStatus==="VERIFIED_NO_CHANGE"),rok=reason==="전체 유형"||normalizedReason(r)===reason;return qok&&sok&&rok}),[rows,query,status,reason]);
 const totalPages=Math.max(1,Math.ceil(filtered.length/PAGE_SIZE)),safePage=Math.min(page,totalPages),pageRows=filtered.slice((safePage-1)*PAGE_SIZE,safePage*PAGE_SIZE),pendingPageIds=pageRows.filter(r=>!r.correctionStatus||r.correctionStatus==='PENDING').map(r=>r.stagingListingId);
 function reset(){setPage(1);setSelected([])}
 function ensureForm(row:CorrectionRow){setForms(prev=>prev[row.stagingListingId]?prev:{...prev,[row.stagingListingId]:initialForm(row)});setNotes(prev=>prev[row.stagingListingId]!==undefined?prev:{...prev,[row.stagingListingId]:row.note??""});setOpenId(prev=>prev===row.stagingListingId?null:row.stagingListingId)}
 async function openPdf(row:CorrectionRow){setBusy(`pdf-${row.stagingListingId}`);const r=await getSignedPdfUrl(sourceDocumentId);setBusy(null);if(r.error||!r.url){alert(r.error||"원본 PDF를 열 수 없습니다.");return}window.open(row.page?`${r.url}#page=${row.page}`:r.url,"_blank","noopener,noreferrer")}
 async function apply(row:CorrectionRow){const form=forms[row.stagingListingId]??initialForm(row);const patch:ListingCorrectionPayload={};for(const key of numberKeys){const raw=String(form[key]??"").replace(/,/g,"").trim();(patch as Record<string,unknown>)[key]=raw===""?null:Number(raw)}patch.floor=String(form.floor??"").trim()||null;patch.unit=String(form.unit??"").trim()||null;patch.move_in_text=String(form.move_in_text??"").trim()||null;patch.status=String(form.status??"available")||"available";patch.is_published=Boolean(form.is_published);if(Object.values(patch).some(v=>typeof v==="number"&&!Number.isFinite(v))){alert("숫자 입력값을 확인해 주세요.");return}setBusy(row.stagingListingId);setMessage(null);const r=await applyListingCorrection(sourceDocumentId,row.stagingListingId,patch,notes[row.stagingListingId]??"");setBusy(null);if(r.error){alert(r.error);return}setMessage(`${row.buildingName} ${row.floor??""} 수정값을 반영했습니다.`);window.location.reload()}
 async function verify(row:CorrectionRow){if(!confirm("원본과 현재 등록값이 맞아서 수정 없이 검토완료 처리할까요?"))return;setBusy(row.stagingListingId);const r=await verifyListingWarningNoChange(sourceDocumentId,row.stagingListingId,notes[row.stagingListingId]??"");setBusy(null);if(r.error){alert(r.error);return}window.location.reload()}
 async function bulkVerify(){if(selected.length===0)return;if(!confirm(`선택한 ${selected.length}건을 현재값 정상으로 일괄 검토완료 처리할까요? 원본을 확인한 동일 유형에만 사용하세요.`))return;setBusy('bulk');const r=await bulkVerifyListingWarningsNoChange(sourceDocumentId,selected,"동일 유형 원본 대조 후 일괄 정상확인");setBusy(null);if(r.error){alert(r.error);return}if(r.failed)alert(`정상확인 ${r.verified??0}건, 실패 ${r.failed}건\n${(r.errors??[]).join('\n')}`);window.location.reload()}
 return <section id="listing-correction-review" className="mb-5 scroll-mt-6 border border-red-200 bg-red-50/40 p-5">
  <div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs uppercase tracking-[.16em] text-red-700">Post-Approval Correction</p><h2 className="mt-1 font-medium">승인 후 오류 매물 정정</h2><p className="mt-1 text-xs leading-5 text-red-900">미검토 항목을 25건씩 나눠 보고, 동일 경고 유형은 묶어서 검토합니다. 일괄 정상확인은 원본을 샘플 대조한 동일 유형에만 사용하세요.</p></div><div className="grid grid-cols-3 gap-2 text-center text-xs"><button onClick={()=>{setStatus('미검토');reset()}} className="border bg-white px-3 py-2"><b>{counts.pending}</b><div>미검토</div></button><button onClick={()=>{setStatus('수정반영');reset()}} className="border bg-white px-3 py-2"><b>{counts.applied}</b><div>수정반영</div></button><button onClick={()=>{setStatus('정상확인');reset()}} className="border bg-white px-3 py-2"><b>{counts.verified}</b><div>정상확인</div></button></div></div>
  {message&&<p className="mt-3 border border-green-200 bg-green-50 p-2 text-xs text-green-800">{message}</p>}
  <div className="sticky top-0 z-20 -mx-2 mt-4 flex flex-wrap items-center gap-2 border-y border-red-100 bg-red-50/95 px-2 py-2 backdrop-blur">{["미검토","전체","수정반영","정상확인"].map(s=><button key={s} onClick={()=>{setStatus(s);reset()}} className={`border px-3 py-1.5 text-xs ${status===s?"border-red-700 bg-red-700 text-white":"border-red-200 bg-white text-red-900"}`}>{s}</button>)}<select value={reason} onChange={e=>{setReason(e.target.value);reset()}} className="max-w-[360px] border border-red-200 bg-white px-3 py-1.5 text-xs">{reasonOptions.map(r=><option key={r}>{r}</option>)}</select><input value={query} onChange={e=>{setQuery(e.target.value);reset()}} placeholder="건물명·층·경고 검색" className="min-w-[220px] flex-1 border border-red-200 bg-white px-3 py-1.5 text-xs outline-none"/></div>
  {status==='미검토'&&<div className="mt-3 flex flex-wrap items-center justify-between gap-2 border border-red-100 bg-white p-3 text-xs"><div><b>현재 페이지 {pendingPageIds.length}건</b> · 선택 {selected.length}건</div><div className="flex gap-2"><button onClick={()=>setSelected(pendingPageIds)} className="border px-3 py-1.5">현재 페이지 전체선택</button><button onClick={()=>setSelected([])} className="border px-3 py-1.5">선택해제</button><button onClick={bulkVerify} disabled={selected.length===0||busy==='bulk'} className="bg-green-700 px-3 py-1.5 text-white disabled:opacity-40">선택 정상확인</button></div></div>}
  <div className="mt-4 space-y-2">{pageRows.map(row=>{const form=forms[row.stagingListingId]??initialForm(row),opened=openId===row.stagingListingId,badge=row.correctionStatus==="APPLIED"?"수정반영":row.correctionStatus==="VERIFIED_NO_CHANGE"?"정상확인":"미검토",pending=!row.correctionStatus||row.correctionStatus==='PENDING';return <div key={row.stagingListingId} className="border border-red-100 bg-white"><div className="flex flex-wrap items-center justify-between gap-3 p-3"><div className="flex items-start gap-3">{pending&&<input type="checkbox" checked={selected.includes(row.stagingListingId)} onChange={e=>setSelected(prev=>e.target.checked?[...prev,row.stagingListingId]:prev.filter(id=>id!==row.stagingListingId))} className="mt-1"/>}<div><div className="flex flex-wrap items-center gap-2"><b>{row.buildingName}</b><span>{row.floor??"층 미상"}</span><span className="border px-2 py-0.5 text-[10px]">{badge}</span></div><p className="mt-1 text-xs text-red-700">{row.reasons.join(" · ")}</p><p className="mt-1 text-[11px] text-silver">PDF {row.page?`p.${row.page}`:"페이지 미상"} · 실제 공실 {row.listingId?"연결됨":"자동 연결 예정"}</p></div></div><div className="flex gap-2"><button onClick={()=>openPdf(row)} disabled={busy===`pdf-${row.stagingListingId}`} className="border px-3 py-1.5 text-xs">원본 페이지</button><button onClick={()=>ensureForm(row)} className="border border-navy px-3 py-1.5 text-xs text-navy">{opened?"닫기":"검수·정정"}</button></div></div>{opened&&<div className="border-t bg-fog/40 p-4"><div className="overflow-auto"><table className="min-w-[1120px] w-full text-xs"><thead><tr><th className="p-2 text-left">항목</th><th className="p-2 text-left">현재 DB</th><th className="p-2 text-left">PDF 추출값</th><th className="p-2 text-left">정정값</th></tr></thead><tbody>{[["floor","층"],["unit","호실"],["gross_area_py","임대면적(평)"],["exclusive_area_py","전용면적(평)"],["rent_per_py","임대료/평"],["maintenance_per_py","관리비/평"],["deposit_total_won","총 보증금(원)"],["monthly_rent_total_won","총 월세(원)"],["management_fee_total_won","총 관리비(원)"],["move_in_text","입주시기"]].map(([key,label])=><tr key={key} className="border-t"><td className="p-2 font-medium">{label}</td><td className="p-2">{format(row.current?.[key as keyof Values])}</td><td className="p-2 text-silver">{format(row.extracted[key as keyof Values])}</td><td className="p-2"><input value={String(form[key]??"")} onChange={e=>setForms(prev=>({...prev,[row.stagingListingId]:{...form,[key]:e.target.value}}))} className="w-full border bg-white px-2 py-1"/></td></tr>)}</tbody></table></div><div className="mt-3 grid gap-3 md:grid-cols-[1fr_auto_auto]"><input value={notes[row.stagingListingId]??""} onChange={e=>setNotes(prev=>({...prev,[row.stagingListingId]:e.target.value}))} placeholder="정정/검토 메모 (선택)" className="border bg-white px-3 py-2 text-xs"/><button disabled={busy===row.stagingListingId} onClick={()=>verify(row)} className="border border-green-700 px-4 py-2 text-xs text-green-700 disabled:opacity-40">현재값 정상 · 검토완료</button><button disabled={busy===row.stagingListingId} onClick={()=>apply(row)} className="bg-charcoal px-4 py-2 text-xs text-white disabled:opacity-40">수정값 개별 반영</button></div></div>}</div>})}{pageRows.length===0&&<p className="border bg-white p-6 text-center text-xs text-silver">조건에 맞는 경고 매물이 없습니다.</p>}</div>
  <div className="mt-3 flex items-center justify-between text-xs"><span>{filtered.length}건 · {safePage}/{totalPages} 페이지</span><div className="flex gap-2"><button disabled={safePage<=1} onClick={()=>{setPage(p=>Math.max(1,p-1));setSelected([])}} className="border px-3 py-1.5 disabled:opacity-30">이전</button><button disabled={safePage>=totalPages} onClick={()=>{setPage(p=>Math.min(totalPages,p+1));setSelected([])}} className="border px-3 py-1.5 disabled:opacity-30">다음</button></div></div>
 </section>
}
