# CORE PRIME v1.20 적용 방법

## 적용
1. 이 ZIP의 내부 파일을 현재 GitHub `core-prime` 프로젝트 폴더에 덮어쓴다.
2. GitHub Desktop에서 변경 파일을 확인한다.
3. Commit 메시지 예: `CORE PRIME v1.20 CBRE enrichment final stability`
4. Commit to main → Push origin
5. Vercel 배포가 `Ready`인지 확인
6. 관리자 페이지에서 Ctrl+F5
7. CBRE 자료 상세에서 `건물 기본정보 보강 실행` 클릭

## 현재 180/233에서 멈춰 있는 기존 실행
기존 v1.17.x 실행은 더 기다릴 필요가 없다. 페이지를 새로고침해도 이미 저장된 값은 유지된다.

v1.20을 배포한 후 다시 실행하면 v1.20 첫 실행은 0/233부터 시작한다. 이는 앞의 180개도 새 Parser로 다시 검증하고, 이전 자동값 중 안전하게 증명 가능한 오류를 교정하기 위한 의도된 동작이다.

이후 v1.20 실행이 네트워크 문제로 중단되면 마지막 완료 배치부터 이어서 재개한다.

## SQL
추가 SQL 없음.

## 변경 범위
- `src/lib/parsers/cbre/CBREParser.ts`
- `src/app/admin/(protected)/imports/actions.ts`
- `src/components/admin/BuildingFactsEnrichmentButton.tsx`
- v1.20 문서 3개

DB 스키마, 기존 승인 데이터 구조, 고객페이지, Prime Score 로직은 변경하지 않는다.
