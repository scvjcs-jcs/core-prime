# CORE PRIME v1.11 — 승인 후 오류 매물 정정·개별 재승인

## 목적
최종 승인된 PDF Import 전체를 다시 실행하지 않고, 경고가 남은 공실만 원본 PDF와 대조하여 개별 정정하거나 정상 확인 처리합니다.

## 관리자 흐름
1. Import 상세 화면에서 `승인 후 오류 매물 정정` 영역 확인
2. `미검토` 항목에서 `원본 페이지` 클릭
3. `검수·정정` 열기
4. 현재 DB / PDF 추출값 / 정정값을 비교
5. 값이 맞으면 `현재값 정상 · 검토완료`
6. 값이 틀리면 정정값 수정 후 `수정값 개별 반영`

## 안전장치
- 관리자만 실행 가능
- staging 1건당 correction review 1건만 유지(upsert)
- 승인 당시 `matched_listing_id`가 없는 NEW 공실도 source_document + 건물 + PDF page + 층 + 호실로 실제 공실을 재연결
- 후보가 0건이면 중단
- 후보가 2건 이상이면 자동 수정 금지
- 수정 전/후 값을 audit_logs에 기록
- 기존 PDF staging/Parser 결과는 삭제하지 않음
- 전체 재승인 없이 해당 canonical listing만 수정

## DB 추가
- `listing_correction_reviews`
- `_resolve_canonical_listing_for_staging(uuid)`
- `apply_listing_correction(uuid,jsonb,text)`
- `verify_listing_warning_no_change(uuid,text)`
