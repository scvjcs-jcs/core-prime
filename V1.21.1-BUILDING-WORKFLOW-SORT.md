# CORE PRIME v1.21.1 — 건물관리 공개/비공개 업무정렬

## 목적
건물관리 화면에서 안전공개 후 공개된 건물이 기존 위치에 남아 비공개 건물과 섞이는 문제를 해결합니다.

## 변경
- 기본 정렬을 `비공개 → 공개`로 변경
- 비공개 그룹 안에서는 `준비완료 → 준비도 점수 높은 순 → 건물명` 순으로 표시
- 안전공개 후 새로고침되면 해당 건물은 자동으로 공개 그룹(목록 뒤쪽)으로 이동
- 정렬은 pagination 전에 적용되므로 50개 페이지를 넘어가도 공개/비공개가 원래 순서 때문에 다시 섞이지 않음
- 선택 가능한 정렬: 업무순서(비공개 먼저), 공개 먼저, 준비도 높은순, 건물명순
- `공개 대기 · 준비 완료` 수치는 이미 공개된 건물을 제외
- `현재 페이지 준비완료 선택`도 이미 공개된 건물을 제외

## DB 변경
없음. SQL 실행 불필요.
