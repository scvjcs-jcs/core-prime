# v1.21 Homepage Launch-Safe UX

## Why
운영 사이트의 공개 건물/공실이 0인 시점에서 기존 메인화면은 `0 / 0`, 빈 히어로 이미지, `기준일 미확인`, 작동은 하지만 결과가 없는 검색폼을 동시에 노출해 미완성처럼 보였다.

## Changes
- 공개 데이터가 1개 이상이면 기존 데이터 중심 메인 화면 유지.
- 공개 데이터가 0이면 숫자 0을 크게 노출하지 않고 `검수 진행중 / 순차 공개 / 상담 가능`으로 전환.
- 대표 건물이 없을 때 빈 그라데이션 대신 CORE PRIME 데이터 워크플로 패널 표시.
- 대표 건물이 없으면 freshness badge 숨김.
- 공개 데이터가 0이면 검색폼 대신 맞춤 후보 요청 CTA 우선 노출.
- 공개 건물 수는 count 쿼리, 공개 건물 조회 결과, featured 결과를 교차해 false-zero를 완화.
- 실제 공개 데이터가 생기는 순간 별도 설정 없이 기존 검색/숫자/대표빌딩 UI로 자동 전환.

## Safety
- 관리자/Parser/PDF/Supabase schema 변경 없음.
- 비공개 건물/매물은 고객 화면에 노출하지 않음.
- 가짜 건물 수/공실 수를 만들지 않음.
