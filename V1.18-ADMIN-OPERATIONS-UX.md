# CORE PRIME v1.18 — 관리자 운영 UX 최적화

## 목표
PDF 등록 → 원문 추출 → 구조화 → 매칭 → 변경 검수 → 승인 → 오류 정정 흐름을 운영자가 한눈에 이해하고, 한 번에 하나의 다음 작업에 집중하도록 정리합니다.

## 변경 사항
### 건물 관리
- 50개 단위 페이지 분할
- 현재 페이지 전체 선택
- 현재 페이지 공개 준비완료 선택
- 현재 페이지 Prime Score 추천 대기 선택
- Prime Score 추천 한도(50개)와 페이지 크기 일치
- 페이지 이동 시 선택 초기화로 오작업 방지
- 상단 자동화 센터 sticky 처리

### PDF 자료 등록
- 2단계 폼: 기본정보 → PDF 선택
- Parser는 자동 추천, 직접 선택은 고급 설정으로 이동
- PDF 선택 시 제목 자동 입력
- 등록 전 처리 흐름을 우측 요약으로 설명
- 등록 버튼을 `PDF 등록하고 검수 시작`으로 명확화

### 자료검수 상세
- 단계별 버튼 4개를 항상 노출하는 대신 `다음 할 일` 1개를 우선 노출
- 현재 단계 강조
- 승인 완료 문서는 재처리 버튼 대신 경고/정정 확인 안내
- 단계별 직접 실행 버튼은 `고급 작업`으로 접음
- 건물 연결 작업함 25개 단위 페이지 분할
- 공실 충돌/종료 검수는 작업함으로 분리
- 승인 완료 후 대형 공실/건물 후보 표는 기본 접힘

### 자료검수 목록
- 각 자료에 `다음 할 일` 표시
- 처리·검수 필요 / 승인 완료 / 남은 경고 요약 카드
- 작업 버튼 문구를 상태에 따라 변경

## DB / SQL
- 추가 SQL 없음
- 기존 데이터와 RPC 변경 없음
