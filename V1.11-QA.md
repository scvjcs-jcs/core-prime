# v1.11 QA

## 정적 점검 결과
- ZIP 기반 원본 프로젝트에서 v1.10.1 기능을 유지하고 correction UI만 추가
- `min(uuid)` 사용 없음
- audit_logs 허용 action(CREATE/UPDATE/DELETE/APPROVE/EXPIRE/MATCH/UNMATCH) 중 UPDATE만 사용
- 관리자 권한 검사 포함
- exact total amount 3개 컬럼 정정 지원
- 동일 staging listing review unique 제약 적용
- canonical listing 자동 재연결 시 0건/복수건을 모두 차단
- 기존 Parser / 건물 매칭 / Diff / 최종 승인 로직은 변경하지 않음

## 시나리오 점검
1. NEW 승인 후 matched_listing_id가 null인 경우: source_document + building + page + floor + unit exact match 1건이면 연결
2. 같은 조건 canonical listing 2건 이상: 자동 정정 중단
3. canonical listing 0건: 자동 정정 중단
4. 현재값 정상 처리: canonical 값 변경 없이 review status=VERIFIED_NO_CHANGE
5. 수정 반영: canonical listing 1건만 UPDATE + review status=APPLIED + audit log
6. 같은 항목 재검수: correction review는 중복 insert 대신 upsert

## 환경 한계
이 작업 환경에는 프로젝트 node_modules가 없어 Next production build 및 실제 운영 Supabase RPC 호출은 실행하지 못했습니다. TypeScript compiler의 구문 단계에서 새 파일의 syntax error는 발견되지 않았고, SQL은 정적 검토했습니다. 운영 적용 후 1개 항목으로 먼저 시험 정정을 권장합니다.
