# CORE PRIME v1.17.1 — CBRE 건물 기본정보 보강 안정화

## 원인
기존 v1.14/v1.17 보강 액션은 200개 이상의 건물에 대해 건물/주차/교통 정보를 한 번의 Server Action에서 순차 처리했습니다. Vercel 환경에서는 장시간 Server Action이 네트워크/실행 제한에 걸릴 수 있고, 클라이언트에서 예외를 잡지 않아 전체 페이지가 `Application error: a client-side exception...` 화면으로 전환될 수 있었습니다.

## 수정
- 12개 건물씩 deterministic batch 처리
- 각 batch마다 서버 액션 종료 후 다음 batch 호출
- 진행률 UI 추가
- client-side try/catch 추가: 실패해도 페이지 전체가 죽지 않고 오류 메시지 표시
- 서버는 limit 최대 20개로 강제
- 재실행 안전: 기존 값은 덮어쓰지 않고 빈 값만 채우므로 중간 실패 후 재실행 가능
- 최종 batch에서만 공개/관리자 목록 revalidate

## DB / SQL
- DB 스키마 변경 없음
- SQL 실행 없음
