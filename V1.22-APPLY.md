# CORE PRIME v1.22 적용 순서

1. Supabase > SQL Editor 열기
2. `SUPABASE-APPLY-v1.22-CONTENT-CENTER.sql` 전체 붙여넣기 후 Run
3. v1.22 ZIP 압축 해제
4. 현재 GitHub core-prime 폴더에 ZIP 안쪽 파일을 덮어쓰기
5. GitHub Desktop > Commit to main
6. Push origin
7. Vercel에서 Ready 확인
8. 관리자 새 메뉴 `콘텐츠` 진입
9. 건물 1개 선택 > `검증 데이터로 초안 만들기`
10. 전체 원고 복사 > 네이버 블로그 열기 > 사진/지도 추가 후 직접 발행
11. 발행된 글 URL을 콘텐츠 센터에 붙여넣고 `발행완료 기록`

## AI 다듬기
기본 초안은 API 키 없이 동작합니다. `AI로 문장만 다듬기` 기능을 쓰려면 기존 ANTHROPIC_API_KEY 설정이 필요합니다.
