# CORE PRIME v1.12 — Prime Score 자동추천 + 관리자 확정

## 목적
Prime Score를 사람이 7개 항목 모두 처음부터 입력하는 대신, 현재 DB의 객관 데이터로 추천점수를 만들고 관리자가 수정·확정합니다.

## 안전 원칙
- 추천점수는 고객에게 자동 공개되지 않습니다.
- `추천점수 초안에 적용` 시 상태는 `DRAFT`입니다.
- 관리자가 7개 항목을 확인하고 저장한 뒤 직접 `PUBLISHED`로 바꿔야 고객에게 표시됩니다.
- 기존 Prime Score 값은 추천 계산만으로 덮어쓰지 않습니다.
- 추천 이력은 별도 `prime_score_recommendations` 테이블에 저장됩니다.

## 추천에 쓰는 데이터
- 입지: 업무권역
- 교통: 역 도보시간, 역 수, 노선 수
- 건물 품질: 준공연도, 등급, 연면적, 전용률
- 주차: 총 주차대수, 연면적 대비 주차, 자주식, EV
- 편의시설: HVAC, EV, 자주식, amenity/lobby 이미지 데이터
- 기업 이미지: 등급, 규모, 층수, 준공연도
- 직원 접근성: 교통 72% + 주차 13% + 권역 15%

누락 데이터는 0점으로 처리하지 않고 보수적인 중립값을 사용합니다. 화면에 `추천 신뢰도`와 `데이터 충족도`를 함께 표시합니다.

## 적용
1. Supabase SQL Editor에서 `SUPABASE-APPLY-v1.12.sql` 실행
2. 프로젝트 파일 덮어쓰기
3. GitHub Desktop Commit / Push
4. Vercel Ready 확인
5. 관리자 > 건물 관리 > 건물 수정 > Prime Score 탭
6. `자동 추천점수 계산`
7. 근거·신뢰도 확인
8. `추천점수 초안에 적용`
9. 필요 시 항목별 수정 후 건물 저장
10. 최종 검토 후 공개 상태를 `PUBLISHED`로 변경
