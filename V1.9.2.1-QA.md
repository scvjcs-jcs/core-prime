# CORE PRIME v1.9.2.1 QA

- ZIP integrity: checked
- Runtime hotfix SQL and migration SQL synchronized
- Removed UUID `min(id)` usage from all active v1.9 bulk-registration SQL files
- RPC signature unchanged: `bulk_create_buildings_from_staging(uuid, uuid[])`
- App RPC call/return keys checked
- Existing CBRE parser, PDF extraction, diff, approval code untouched
- Bulk registration safeguards retained: admin only, max 300, NEW_CANDIDATE only, source-document advisory lock, `FOR UPDATE`, exact road-address dedupe first, normalized-name-only fallback only when staging address absent, ambiguous/mismatched-name-address routed to REVIEW_REQUIRED, new buildings unpublished

Note: live Supabase execution still requires project-side verification because this package does not have credentials to the production database.
