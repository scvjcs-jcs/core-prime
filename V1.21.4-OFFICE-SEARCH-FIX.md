# CORE PRIME v1.21.4 — 업무권역 검색 수정

## 원인
1. PDF import로 신규 생성된 buildings 레코드에는 `district_id`가 자동 저장되지 않았다.
2. 메인 검색은 사용자 선택과 별개로 `availableOnly=1`을 강제로 붙여, 공개 건물이어도 공개 공실이 없으면 검색결과에서 제외됐다.

## 수정
- 기존 district_id가 비어 있는 건물을 주소/건물명 기반으로 자동 매핑하는 SQL 추가.
- 신규 건물도 district_id가 비어 있으면 자동 추론하는 DB trigger 추가.
- 수동 지정 district_id는 덮어쓰지 않음.
- 메인 검색의 `availableOnly=1` 강제 조건 제거.
- 빠른 권역 링크에서도 `availableOnly=1` 제거.
- `/buildings`의 “현재 공실이 있는 건물만 보기” 체크박스는 그대로 유지하여 사용자가 원할 때만 적용.

## 적용 순서
1. `SUPABASE-APPLY-v1.21.4-DISTRICT-SEARCH-FIX.sql` 실행
2. 프로젝트 파일 덮어쓰기
3. Git commit / push
4. Vercel Ready 후 강력 새로고침
5. 오피스 찾기 > 업무권역 선택 > 검색 확인
